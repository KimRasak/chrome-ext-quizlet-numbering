# chrome-ext-quizlet-numbering

Chrome extension that adds sequence number labels to Quizlet word lists, skips inserting labels into editable fields while editing, and provides an in-page floating tool for jumping to a word number or exporting every visible word to a `.txt` file. For long lists, the jump action now keeps scrolling to load more rows until the target appears or the end of the list is reached.
